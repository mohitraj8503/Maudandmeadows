"""Original mongo_seed_gallery.py moved to scripts_disabled for safekeeping.
Run only if you intentionally want to seed sample gallery items.
"""
from pathlib import Path
print("mongo_seed_gallery.py has been moved to scripts_disabled/ and should not be run by default.")
print(f"Path: {Path(__file__).as_posix()}")
